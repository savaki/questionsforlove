<!DOCTYPE html>
<html>
<head>
    <title>Questions for Love</title>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-touch-fullscreen" content="yes">
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, user-scalable=no"/>

    <!-- jQuery & jQuery Mobile -->
    <script src="//code.jquery.com/jquery-1.11.2.min.js"></script>
    <script src="//code.jquery.com/mobile/git/jquery.mobile-git.js"></script>
    <link rel="stylesheet" href="//code.jquery.com/mobile/git/jquery.mobile-git.css"/>
    <!--<link href='http://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>-->
    <link href='http://fonts.googleapis.com/css?family=Droid+Serif' rel='stylesheet' type='text/css'>

    <script type="text/javascript">
        (function (h, a) {
            var f = h.amplitude || {};
            var b = a.createElement("script");
            b.type = "text/javascript";
            b.async = true;
            b.src = "https://d24n15hnbwhuhn.cloudfront.net/libs/amplitude-2.0.2-min.js";
            var g = a.getElementsByTagName("script")[0];
            g.parentNode.insertBefore(b, g);
            f._q = [];
            function e(i) {
                f[i] = function () {
                    f._q.push([i].concat(Array.prototype.slice.call(arguments, 0)))
                }
            }

            var c = ["init", "logEvent", "setUserId", "setUserProperties", "setVersionName", "setDomain", "setDeviceId",
                "setGlobalUserProperties"];
            for (var d = 0; d < c.length; d++) {
                e(c[d])
            }
            h.amplitude = f
        })(window, document);

        amplitude.init("f73316fa082c02616f29d64b31012453");
    </script>

    <style type="text/css">
        /* Prevent text selection while swiping with mouse */
        h1, p {
            font-family: 'Droid Serif', sans-serif;
            user-select: none;
        }

        h1 {
            text-align: center;
            padding: 0 25px;
        }

        .intro h1 {
            margin-top: 90px;
        }

        h1.progress {
            position: absolute;
            bottom: -10px;
            font-weight: bold;
            font-size: 26px;
            color: #bbb;
        }

        p {
            position: relative;
            margin-top: 90px;
            padding: 5px 25px;
            font-size: 26px;
            line-height: 36px;
        }

        .intro p {
            margin-top: inherit;
            font-size: 24px;
            line-height: inherit;
            text-align: center;
        }

        p.call-to-action {
            margin-top: 50px;
            margin-bottom: 50px;
        }

        p.inspiration {
            margin-top: 110px;
            font-size: 16px;
            color: #333;
        }

        html, body {
            height: 100%;
            width: 100%;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }
    </style>

    <script>
        <!--
        function t(nextpage) {
            var header = $(nextpage[0]).find("h1")
            if (header != undefined) {
                amplitude.logEvent(header.text());
            }
        }

        $(document).ready(function () {
            amplitude.logEvent("Intro");
        });

        $(document).on("swipeleft", 'div.page', function () {
            var nextpage = $(this).next('div[data-role="page"]');
            if (nextpage.length > 0) {
                t(nextpage);
                $.mobile.changePage(nextpage, {transition: "slide"}, false, true);
            }
        });
        $(document).on("swiperight", 'div.page', function () {
            var prevpage = $(this).prev('div[data-role="page"]');
            if (prevpage.length > 0) {
                $.mobile.changePage(prevpage, {
                    transition: "slide",
                    reverse: true
                }, true, true);
            }
        });
        // -->
    </script>
</head>
<body>
<div data-role="page" class="page intro">
    <h1>Questions for Love</h1>

    <p>Thirty six questions to bring you and your partner closer</p>

    <p class="call-to-action">Swipe to begin</p>

    <p class="inspiration">Inspired by the article, <a
    href="http://www.nytimes.com/2015/01/11/fashion/modern-love-to-fall-in-love-with-anyone-do-this.html?_r=0">To Fall
        in Love With Anyone, Do This</a>. Based on the work of Arthur Aron,
        <a href="http://www.stafforini.com/txt/Aron%20et%20al%20-%20The%20experimental%20generation%20of%20interpersonal%20closeness.pdf">The
            Experimental Generation of Interpersonal Closeness</a></p>

</div>

<% sets.keys.sort.each do |key| %>
    <div data-role="page" class="page">
        <h1><%= key %></h1>
    </div>
    <% sets[key].each do |question| %>
        <% num, text = question.to_s.split(/\s/, 2) %>
        <div data-role="page" class="page">
            <h1 class="progress"><%= num.gsub(/\./, '') %>/36</h1>

            <p><%= text %></p>
        </div>
    <% end %>
<% end %>
<div data-role="page" class="page">
    <h1>The End</h1>
</div>
</body>
</html>

